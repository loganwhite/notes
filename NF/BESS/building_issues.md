**Sometimes, when building bess, it will prompts errors like cannot find function of dpdk...**

One possible solution is to determine if the DPDK environment variable is set correctly. (e.g. ```RTE_SDK```, ```RTE_TARGET```)
